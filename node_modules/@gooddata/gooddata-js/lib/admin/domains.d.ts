import { XhrModule } from "../xhr";
export declare class DomainsModule {
    private xhr;
    constructor(xhr: XhrModule);
    getDomain(contractId: string, domainId: string, query: string | null): Promise<any>;
    getDomains(contractId: string, query: string | null): Promise<{
        items: any;
    }>;
    getDomainUsers(contractId: string, domainId: string, query: any, paging: any): Promise<any>;
    getDomainProjects(contractId: string, domainId: string, state: any, query: any, paging: any): Promise<any>;
}
