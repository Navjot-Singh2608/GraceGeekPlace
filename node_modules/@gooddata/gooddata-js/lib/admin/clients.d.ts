import { XhrModule } from "../xhr";
export declare class ClientsModule {
    private xhr;
    constructor(xhr: XhrModule);
    getClient(contractId: string, dataProductId: string, segmentId: string, domainId: string, clientId: string): Promise<any>;
    getClients(contractId: string, dataProductId: string, segmentId: string, domainId: string, filter: any, paging: any): Promise<{
        items: any;
        paging: any;
    }>;
    getClientUsers(contractId: string, dataProductId: string, domainId: string, segmentId: string, clientId: string, query: any, paging: any): Promise<any>;
    private transformClient;
    private transformClientUser;
}
