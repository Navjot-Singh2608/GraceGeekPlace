import { XhrModule } from "../xhr";
export declare class LogsModule {
    private xhr;
    constructor(xhr: XhrModule);
    getLogs(contractId: string, dataProductId: string, domainId: string, segmentId: string): Promise<any>;
}
